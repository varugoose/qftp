# QFTP Phase 3 dependencies
#
# aioquic provides the QUIC transport (RFC 9000) + TLS 1.3 (RFC 9001) underneath
# QFTP. It is the ONLY third-party component touching the network; all QFTP
# message framing, serialization, parsing, and the DFA are hand-written.
aioquic>=1.0.0

# Test runner for the PDU / DFA unit tests.
pytest>=7.0.0
